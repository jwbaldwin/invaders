<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "scoreboard-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :score %>
  <%= number_input f, :score %>
  <%= error_tag f, :score %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
